  apps:[

    { - name: "Health & Wellness - pusher 3009"
          script: artisan
          exec_mode: fork
          interpreter: php
          instances: 1
          args:
            - queue:work
            - --queue=high,default
    }
    {
      "name": "Health & Wellness schedule Work",
      "script": "artisan",
      "args": [ "schedule:work" ],
      "instances": "1",
      "wait_ready": true,
      "autorestart": true,
      "interpreter": "php",
      "time": true
    }

  ]
